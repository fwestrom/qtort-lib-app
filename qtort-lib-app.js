/**
 * A utility for creating applications that provides lifecyle events,
 * command line arguments, and other useful features that are commonly
 * needed for managing the application lifecycle.
 * @module qtort-lib-app
 */

'use strict';
let app = require('./lib/app');

module.exports = app.App;
